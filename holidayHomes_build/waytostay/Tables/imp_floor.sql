CREATE TABLE [waytostay].[imp_floor] (
    [sourceId]  INT            NOT NULL,
    [runId]     INT            NOT NULL,
    [fileId]    INT            NOT NULL,
    [floor]     NVARCHAR (255) NULL,
    [floors_id] INT            NOT NULL
);

