CREATE TABLE [waytostay].[imp_media] (
    [sourceId]     INT NOT NULL,
    [runId]        INT NOT NULL,
    [fileId]       INT NOT NULL,
    [media_id]     INT NOT NULL,
    [apartment_id] INT NOT NULL
);

