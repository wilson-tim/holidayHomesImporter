CREATE TABLE [waytostay].[imp_apartment] (
    [sourceId]     INT NOT NULL,
    [runId]        INT NOT NULL,
    [fileId]       INT NOT NULL,
    [apartment_id] INT NOT NULL,
    [id]           INT NOT NULL
);

