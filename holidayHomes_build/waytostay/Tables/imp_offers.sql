CREATE TABLE [waytostay].[imp_offers] (
    [sourceId]     INT NOT NULL,
    [runId]        INT NOT NULL,
    [fileId]       INT NOT NULL,
    [offers_id]    INT NOT NULL,
    [apartment_id] INT NULL
);

